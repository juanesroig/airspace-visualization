# Base URL of the backend that proxies the OpenSky API over SSE.
# No trailing slash. Leave unset to use the local dev default (http://localhost:5000).
# Required for any deployed build — a production frontend cannot reach localhost.
VITE_API_URL=https://your-backend.example.com
